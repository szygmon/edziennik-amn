
                    <div class="box-body">
                        <ul class="todo-list">
                            {% for c in classrooms %}
                                <li>
                                    <!-- drag handle -->
                                    <span class="handle">
                                        <i class="fa fa-ellipsis-v"></i>
                                        <i class="fa fa-ellipsis-v"></i>
                                    </span>
                                    <!-- checkbox -->
                                    <input type="checkbox" value="" name=""/>
                                    <!-- todo text -->
                                    <span class="text">{{ c.name }}</span>
                                    <!-- General tools such as edit or delete-->
                                    <div class="tools">
                                        <a href="{{ url('School/classrooms', {'action': 'del', 'id': c.id}) }}" class="fa fa-trash-o"></a>
                                    </div>
                                </li>
                            {% else %}
                                <li>
                                    <span class="text">Brak sal lekcyjnych w bazie</span>
                                </li>
                            {% endfor %}
                        </ul>
                    </div><!-- /.box-body -->

                    <div class="box-footer clearfix no-border">
                        <a href="{{ url('School/classroomPrepare') }}" class="btn btn-default pull-right"><i class="fa fa-plus"></i> Dodaj nową</a>
                    </div>
               