{% extends './base.html.twig' %}

{% block title %}eDziennik AMN - grupy{% endblock %}

{% block content %}
    <div class="content-wrapper">
        <!-- Content Header (Page header) -->
        <section class="content-header">
            <h1>
                Szkoła
                <small>Control panel</small>
            </h1>
            <ol class="breadcrumb">
                <li><a href="{{ url('Home/index') }}"><i class="fa fa-dashboard"></i> Home</a></li>
                <li><a href="{{ url('School/index') }}"><i class="fa"></i>Szkoła</a></li>
                <li><a href="{{ url('School/groups') }}"><i class="fa"></i>Grupy</a></li>
                <li class="active">Dodaj nową</li>
            </ol>
        </section>

        <!-- Main content -->
        <section class="content">
            <div class="box box-primary">
                <div class="box-header">
                    <i class="ion ion-clipboard"></i>
                    <h3 class="box-title">Dodaj grupę</h3>
                </div><!-- /.box-header -->
                <div class="box-body">

                    <form name="inputform" method="POST" action="{{ url('School/groups', {'action': 'add'}) }}">
                        <div class="input-group">
                            <span class="input-group-addon" id="basic-addon1">Grupa nadrzędna:</span>

                            <select name="mainGroup" class="form-control" style="width:250px">
                                <option value="0">-brak-</option>
                                {% for gr in groups %}
                                    <option value="{{ gr.id }}">
                                        {% if gr.level > 0 %}
                                            {% for i in range(1, gr.level) %}&nbsp;&nbsp;&nbsp;{% endfor %}
                                        {% endif %}
                                        {{ gr.name }}
                                    </option>
                                {% endfor %}
                            </select>
                        </div>

                        <div class="input-group">
                            <span class="input-group-addon" >Nazwa grupy:</span>
                            <input type="text" name="name" value="" class="form-control" style="width:280px;" />
                        </div>

                        <div class="box-footer clearfix no-border">
                            <input type="submit" name="save" value="Zapisz" class="btn btn-default pull-right" />
                        </div>
                    </form>
                </div><!-- /.box-body -->




            </div><!-- /.box -->


        </section><!-- /.Left col -->

    </div><!-- /.row (main row) -->

</section><!-- /.content -->
</div><!-- /.content-wrapper -->

{% endblock %}
